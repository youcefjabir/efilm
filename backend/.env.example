# Viewly Motion Backend Configuration

# Port to run the server on (default: 3001)
PORT=3001

# Higgsfield API key — get this from your Higgsfield account
# If not set, the backend runs in mock mode (for testing)
HIGGSFIELD_API_KEY=your_api_key_here

# Node environment (development or production)
NODE_ENV=development
